---
import Base from '@/templates/Base.astro';

const { title } = { title: 'Projects' };
const { description } = { description: 'Projects that I have worked on' };

---
<Base head={{ title, description }}>
  <iframe src='/data/Devin-Kelly-Resume.pdf' class=' w-full min-h-screen'/>
</Base>
